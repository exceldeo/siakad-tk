<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.siakad.siakadtk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProductUniformDetailActivity"></activity>
        <activity android:name=".ui.signup.SignupActivity" />
        <activity android:name=".ui.order.OrderDetailActivity" />
        <activity android:name=".HistoryDetailActivity" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".ProfileActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".BasketActivity" />
        <activity android:name=".ProductBookDetailActivity" />
        <activity android:name=".ProductListActivity" />
        <activity android:name=".ui.order.OrderListActivity" />
        <activity android:name=".RegistrationFormActivity" />
        <activity android:name=".AnnouncementListActivity" />
        <activity android:name=".WelcomeScreenActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".SplashScreenActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>